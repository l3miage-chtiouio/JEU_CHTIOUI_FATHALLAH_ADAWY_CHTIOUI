namespace SnakeMonoGame.CSharp;

/* L'énumération Direction définit les quatre directions possibles du serpent :
   UP (haut), DOWN (bas), LEFT (gauche), RIGHT (droite). */
public enum Direction {
    UP = 1,
    DOWN = 2,
    LEFT = 3,
    RIGHT = 4
}